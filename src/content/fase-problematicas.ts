// Fase "Problemáticas" — problematização do tema.
// Content is filled in by a later prompt; empty for now.

import type { ActivityData } from '../engine/types';

export const faseProblematicasActivities: ActivityData[] = [];
