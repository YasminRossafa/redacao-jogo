// Fase "Tema + Brasil" — contextualização do tema na sociedade brasileira.
// Content is filled in by a later prompt; empty for now.

import type { ActivityData } from '../engine/types';

export const faseTemaBrasilActivities: ActivityData[] = [];
